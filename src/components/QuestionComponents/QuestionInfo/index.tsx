import { FC } from 'react';

export type PropsType = {
  title?: string;
  desc?: string;

  disabled: boolean;
};

const QuestionInfo: FC = () => {
  return <>QuestionInfo</>;
};

export default QuestionInfo;
